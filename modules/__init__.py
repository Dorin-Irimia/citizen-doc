"""
Container for optional visual/feature modules.

If the entire ``modules`` folder is removed, the core app continues to work
without import errors. Each subfolder is a standalone Django app that can be
mounted automatically when present.
"""

